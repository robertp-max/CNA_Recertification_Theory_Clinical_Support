// Reviewer/stakeholder demo credential gate (preview only — NOT production auth).
// Preserved verbatim from the approved login behavior.
export const reviewerCredentials = { username: "admin", password: "1234" };

export type LoginFormState = { username: string; password: string };
