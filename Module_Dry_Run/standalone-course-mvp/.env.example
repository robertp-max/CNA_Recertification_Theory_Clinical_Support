DEMO_LOGIN_ENABLED=true
DEMO_LOGIN_EMAIL=admin@careindeed.com
DEMO_LOGIN_PASSWORD=Caregiver2012!
DEMO_LOGIN_HINTS=false

# Optional Vite-only visual fallback for local UI review.
# These values are exposed to the browser and must never be treated as secure.
# If omitted, the app uses the same demo/staging fallback credential for local Vite review.
VITE_DEMO_LOGIN_ENABLED=true
VITE_DEMO_LOGIN_EMAIL=admin@careindeed.com
VITE_DEMO_LOGIN_PASSWORD=Caregiver2012!
VITE_DEMO_LOGIN_HINTS=false
