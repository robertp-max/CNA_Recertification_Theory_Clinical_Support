# Next Build Step

## Prototype Build 1 - Certificate Gate Proof of Concept

Immediate next action: configure a Moodle staging shell that proves certificate release can be blocked until all required online CE gates are complete, while optional clinical support remains outside the certificate gate.

Do not build the full course, issue production certificates, finalize lesson content, add custom Moodle plugins, collect PHI, or treat optional clinical support as required renewal clinical hours.

Start with:

1. Create the staging course category.
2. Create the theory course shell.
3. Add Module 0 orientation and one sample required theory module.
4. Configure required profile fields, completion tracking, Restrict Access, active-time validation, final exam/test placeholder, affidavit placeholder, and certificate activity as disabled or gated.
5. Add one optional clinical support section that is visibly and technically separate.
6. Run the Prototype 1 QA tracker and audit export dry run.

Primary staging packet: `staging/prototype_1_certificate_gate_poc/`.
